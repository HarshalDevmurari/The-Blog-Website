<%- include('partials/header') -%>

<h1 class="home">ADMIN PAGE</h1>

    <% posts.forEach(function(post){ %>

        <h2> <%= post.title %> </h2>
        <p> <%= post.content.substring(0, 30) + " ... " %> 
        <a href="" >Edit</a>
        </p>

   <% }) %>

<%- include('partials/footer') -%>